name: Go SAST CodeQL

permissions:
  contents: read
  security-events: write

on:
  push:
    branches: [main]
    paths: ['**.go', 'go.mod', 'go.sum', '.github/workflows/go-sast-codeql.yaml']
  pull_request:
    branches: [main]
    paths: ['**.go', 'go.mod', 'go.sum', '.github/workflows/go-sast-codeql.yaml']
  schedule:
    - cron: '30 1 * * 0'
  workflow_dispatch:

jobs:
  sast:
    uses: plexusone/.github/.github/workflows/go-sast-codeql.yaml@main
